FlowLedger uses one canonical Supabase migration history.

All schema changes live in `/supabase/migrations` at the repository root. Do not add a second migration history under the mobile application.
